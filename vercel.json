{
  "buildCommand": "pip install -r requirements.txt",
  "devCommand": "python -m flask --app src.app run --debug",
  "functions": {
    "api/index.py": {
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
